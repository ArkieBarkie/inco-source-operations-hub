'use client';import {useMemo,useState} from 'react';import {useOperations} from '@/components/operations-provider';import {ActivityForm} from '@/components/activity-form';import {ActivityList} from '@/components/activity-list';import {PageTitle} from '@/components/ui';import {activityWarnings,formatDate,today} from '@/lib/operations';import type {PlannedActivity} from '@/types/operations';
const monday=(value:string)=>{const d=new Date(`${value}T12:00:00`),day=d.getDay();d.setDate(d.getDate()-(day===0?6:day-1));return d};
export default function Planning(){const {data}=useOperations();const [mode,setMode]=useState<'week'|'day'>('week'),[selected,setSelected]=useState(today()),[form,setForm]=useState(false),[type,setType]=useState(''),[location,setLocation]=useState(''),[status,setStatus]=useState(''),[responsible,setResponsible]=useState('');const start=monday(selected),days=Array.from({length:5},(_,i)=>{const d=new Date(start);d.setDate(d.getDate()+i);return d.toISOString().slice(0,10)});const filtered=useMemo(()=>data.activities.filter(a=>(!type||a.activityType===type)&&(!location||a.originLocation===location||a.destinationLocation===location)&&(!status||a.status===status)&&(!responsible||a.responsibleEmployee===responsible)),[data.activities,type,location,status,responsible]);const warnings=activityWarnings(filtered.filter(x=>days.includes(x.date)));return <div className="container-page"><PageTitle eyebrow="Eenvoudig plannen" title="Planning" description="Leveringen, ophalingen, klantbezorgingen en voorraadverplaatsingen in één overzicht." action={<button onClick={()=>setForm(true)} className="rounded-xl bg-navy px-5 py-3 font-bold text-white">+ Activiteit toevoegen</button>}/><div className="card p-4"><div className="grid gap-3 sm:grid-cols-2 lg:grid-cols-6"><input type="date" value={selected} onChange={e=>setSelected(e.target.value)} className="rounded-xl border px-3 py-2"/><select value={type} onChange={e=>setType(e.target.value)} className="rounded-xl border bg-white px-3 py-2"><option value="">Alle typen</option>{[...new Set(data.activities.map(x=>x.activityType))].map(x=><option key={x}>{x}</option>)}</select><select value={location} onChange={e=>setLocation(e.target.value)} className="rounded-xl border bg-white px-3 py-2"><option value="">Alle locaties</option>{data.locations.map(x=><option key={x.id}>{x.name}</option>)}</select><select value={status} onChange={e=>setStatus(e.target.value)} className="rounded-xl border bg-white px-3 py-2"><option value="">Alle statussen</option>{[...new Set(data.activities.map(x=>x.status))].map(x=><option key={x}>{x}</option>)}</select><select value={responsible} onChange={e=>setResponsible(e.target.value)} className="rounded-xl border bg-white px-3 py-2"><option value="">Iedereen</option><option>Jorn</option><option>Hidde</option><option>Jorn / Hidde</option></select><div className="flex rounded-xl bg-slate-100 p-1"><button onClick={()=>setMode('week')} className={`flex-1 rounded-lg text-sm ${mode==='week'?'bg-white shadow':''}`}>Week</button><button onClick={()=>setMode('day')} className={`flex-1 rounded-lg text-sm ${mode==='day'?'bg-white shadow':''}`}>Dag</button></div></div></div>{warnings.length>0&&<div className="mt-5 rounded-2xl border border-amber-200 bg-amber-50 p-4"><b className="text-amber-900">Aandachtspunten</b><div className="mt-2 flex flex-wrap gap-2">{warnings.map(x=><span key={x} className="rounded-lg bg-white px-3 py-1 text-xs text-amber-800">{x}</span>)}</div></div>}{mode==='week'?<div className="mt-6 grid gap-4 md:grid-cols-2 xl:grid-cols-3 2xl:grid-cols-5">{days.map(d=><Day key={d} date={d} items={filtered.filter(x=>x.date===d)}/>)}</div>:<div className="mt-6"><h2 className="mb-4 text-xl font-bold text-navy">{formatDate(selected)}</h2><ActivityList items={filtered.filter(x=>x.date===selected)} editable/></div>}{form&&<ActivityForm onClose={()=>setForm(false)}/>}</div>}
function Day({date,items}:{date:string;items:PlannedActivity[]}){return <section><div className={`mb-3 rounded-xl p-3 ${date===today()?'bg-violet-100 text-violet-900':'bg-white'}`}><b className="capitalize">{formatDate(date)}</b><div className="text-xs">{items.length} activiteiten</div></div><ActivityList items={items} compact editable/></section>}
