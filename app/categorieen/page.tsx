import Link from 'next/link';import {allSops,categories} from '@/lib/data';
export default function Categorieen(){return <div className="container-page"><p className="label">Automatisch gegroepeerd</p><h1 className="mt-2 text-3xl font-bold text-navy">Categorieën</h1><p className="mb-8 mt-3 text-slate-600">Categorieën zijn rechtstreeks afgeleid van de functionele procesgebieden in de bronbestanden.</p><div className="grid gap-5 md:grid-cols-2 xl:grid-cols-3">{categories.map(c=>{const xs=allSops.filter(s=>s.category===c);return <section key={c} className="card p-6"><div className="flex justify-between"><h2 className="text-lg font-bold text-navy">{c}</h2><span className="text-sm font-bold text-accent">{xs.length}</span></div><div className="mt-5 space-y-2">{xs.slice(0,5).map(s=><Link className="block text-sm text-slate-600 hover:text-accent" key={s.id} href={`/sops/${s.slug}`}>{s.sopNumber} · {s.title}</Link>)}</div>{xs.length>5&&<Link className="mt-5 block text-sm font-bold text-accent" href="/sops">Alle {xs.length} bekijken →</Link>}</section>})}</div></div>}
