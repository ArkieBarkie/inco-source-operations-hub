import Link from 'next/link';import {allSops,processes} from '@/lib/data';
export default function Processen(){return <div className="container-page"><p className="label">Proceslandschap</p><h1 className="mt-2 text-3xl font-bold text-navy">Processen</h1><p className="mb-8 mt-3 text-slate-600">Groepering op basis van de procesgebieden in de bestaande SOP-documentatie.</p><div className="grid gap-6 lg:grid-cols-2">{processes.map(p=>{const xs=allSops.filter(s=>s.process===p);return <section className="card p-7" key={p}><div className="flex items-start justify-between gap-4"><div><div className="label">Proces</div><h2 className="mt-2 text-xl font-bold text-navy">{p}</h2></div><span className="rounded-full bg-blue-50 px-3 py-1 text-sm font-bold text-accent">{xs.length} SOP’s</span></div><dl className="mt-5 grid gap-4 border-y py-5 text-sm sm:grid-cols-2"><div><dt className="text-slate-500">Proceseigenaar</dt><dd className="mt-1 font-bold">{[...new Set(xs.map(x=>x.owner))].join(', ')}</dd></div><div><dt className="text-slate-500">Betrokken afdelingen</dt><dd className="mt-1 font-bold">{[...new Set(xs.map(x=>x.department))].join(', ')}</dd></div></dl><p className="mt-5 text-sm text-slate-500">Procesbeschrijving: Niet vastgelegd als afzonderlijke tekst; onderstaande SOP’s vormen samen dit procesgebied.</p><div className="mt-5 space-y-2">{xs.map(s=><Link key={s.id} href={`/sops/${s.slug}`} className="flex justify-between rounded-xl bg-slate-50 p-3 text-sm hover:bg-blue-50"><span className="font-semibold text-navy">{s.title}</span><span className="ml-3 shrink-0 font-bold text-accent">{s.sopNumber}</span></Link>)}</div><div className="mt-5 text-sm text-slate-500">Knelpunten / verbeterpunten: Niet vastgelegd</div></section>})}</div></div>}
