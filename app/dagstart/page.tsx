'use client';import {useState} from 'react';import {useOperations} from '@/components/operations-provider';import {ActivityList} from '@/components/activity-list';import {ActivityForm} from '@/components/activity-form';import {ActionForm} from '@/components/action-form';import {StatusBadge} from '@/components/ui';import {activityWarnings,formatDate,openAction,today} from '@/lib/operations';import Link from 'next/link';
export default function Dagstart(){const {data}=useOperations();const [activity,setActivity]=useState(false),[action,setAction]=useState<'Actie'|'Afwijking'|null>(null);const date=today(),activities=data.activities.filter(x=>x.date===date),actions=data.actions.filter(openAction),warnings=activityWarnings(activities),incoming=activities.filter(x=>x.activityType==='Leverancierslevering'),outgoing=activities.filter(x=>['Ophaling','Klantbezorging'].includes(x.activityType));return <div className="container-page"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="label">Gezamenlijke dagstart</p><h1 className="mt-2 text-4xl font-bold text-navy">Vandaag · {formatDate(date)}</h1><p className="mt-2 text-slate-600">In vijf minuten: wat komt binnen, wat gaat uit en waar moeten we achteraan?</p></div><div className="flex flex-wrap gap-2"><button onClick={()=>setActivity(true)} className="rounded-xl bg-navy px-4 py-3 text-sm font-bold text-white">+ Activiteit</button><button onClick={()=>setAction('Afwijking')} className="rounded-xl bg-red-50 px-4 py-3 text-sm font-bold text-red-700">Afwijking melden</button></div></div><div className="mt-7 grid grid-cols-2 gap-3 lg:grid-cols-5">{[[incoming.length,'Leveringen'],[outgoing.length,'Uitgaand'],[actions.length,'Open acties'],[actions.filter(x=>x.priority==='Kritiek'||x.priority==='Hoog').length,'Hoge prioriteit'],[activities.filter(x=>x.status==='Vertraagd').length,'Vertraagd']].map(([n,l])=><div className="card p-4" key={l}><div className="text-2xl font-bold text-navy">{n}</div><div className="mt-1 text-xs text-slate-500">{l}</div></div>)}</div><div className="mt-7 grid gap-6 xl:grid-cols-3"><section className="xl:col-span-2"><h2 className="mb-4 text-xl font-bold text-navy">Planning van vandaag</h2><ActivityList items={activities} editable/></section><aside className="space-y-6"><section className="card p-5"><div className="flex justify-between"><h2 className="font-bold text-navy">Openstaande acties</h2><button onClick={()=>setAction('Actie')} className="text-sm font-bold text-accent">+ Actie</button></div>{actions.length?<div className="mt-4 space-y-3">{actions.slice(0,6).map(x=><div key={x.id} className="rounded-xl bg-slate-50 p-3"><div className="flex items-center justify-between gap-2"><b className="text-sm">{x.title}</b><StatusBadge value={x.priority}/></div><p className="mt-1 text-xs text-slate-500">{x.owner} · {x.dueDate}</p></div>)}</div>:<p className="mt-4 text-sm leading-6 text-slate-500">Geen open acties. Leg afspraken met leveranciers of vervoerders direct vast zodra opvolging nodig is.</p>}<Link href="/acties" className="mt-4 block text-sm font-bold text-accent">Alle acties →</Link></section>{warnings.length>0&&<section className="card border-amber-200 bg-amber-50 p-5"><h2 className="font-bold text-amber-900">Nog controleren</h2><ul className="mt-3 space-y-2 text-sm text-amber-800">{warnings.map(x=><li key={x}>• {x}</li>)}</ul></section>}<Link href="/magazijnbeslissing" className="block rounded-2xl bg-navy p-5 text-white"><p className="text-xs font-bold uppercase tracking-widest text-blue-300">Beslismoment</p><h2 className="mt-2 font-bold">Tijdrovende goederenstroom?</h2><p className="mt-2 text-sm text-blue-100">Vergelijk intern met het extern magazijn / 3PL voordat commerciële tijd verdwijnt.</p></Link></aside></div>{activity&&<ActivityForm onClose={()=>setActivity(false)}/>} {action&&<ActionForm type={action} onClose={()=>setAction(null)}/>}</div>}
